# REROUTE Configuration for {{ project_name }}
# Generated by REROUTE
#
# After setting up this file, install dependencies:
#   {{ install_cmd }}

# Application Settings
REROUTE_DEBUG=False
REROUTE_HOST=127.0.0.1
REROUTE_PORT=7376
{% if db_type -%}

# Database Configuration
REROUTE_DATABASE_URL={{ db_url }}

# SECURITY: Never commit real credentials to version control
# Use environment variables for production
# Format examples (DO NOT use these examples directly):
{% if db_type == 'postgresql' -%}
#   postgresql://user:password@localhost:5432/{{ project_name }}
{% elif db_type == 'mysql' -%}
#   mysql+pymysql://user:password@localhost:3306/{{ project_name }}
{% elif db_type == 'mongodb' -%}
#   mongodb://localhost:27017/{{ project_name }}
{% elif db_type == 'sqlite' -%}
#   sqlite:///./{{ project_name }}.db
{% endif -%}
{% endif -%}

# Security (Generate a new secret for production!)
REROUTE_SECRET_KEY=your-secret-key-change-in-production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"

# CORS Settings
# SECURITY: Default origins are restrictive. Add your domains for production.
REROUTE_ENABLE_CORS=True
REROUTE_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000
# For production:
#   REROUTE_CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com
