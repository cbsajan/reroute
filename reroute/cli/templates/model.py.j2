"""
{{ model_name }} Pydantic Models

Generated by REROUTE CLI - Customize the fields below for your needs
"""

from typing import Optional
from datetime import datetime
from pydantic import BaseModel, Field, EmailStr


class {{ model_name }}Base(BaseModel):
    """
    Base {{ model_name }} schema with common fields.

    TODO: Customize these fields based on your requirements.
    Common field types:
    - str: Text fields
    - int: Integer numbers
    - float: Decimal numbers
    - bool: True/False values
    - datetime: Timestamps
    - EmailStr: Email validation (requires pydantic[email])
    - Optional[type]: Makes field optional

    Example:
        name: str = Field(..., description="Full name", min_length=1, max_length=100)
        email: EmailStr = Field(..., description="Email address")
        age: Optional[int] = Field(None, description="Age", ge=0, le=150)
    """
    name: str = Field(..., description="Name", min_length=1, max_length=100)
    description: Optional[str] = Field(None, description="Description")
    is_active: bool = Field(True, description="Active status")


class {{ model_name }}Create({{ model_name }}Base):
    """
    Schema for creating a new {{ model_name }}.
    Used in POST requests.

    Inherits all fields from {{ model_name }}Base.
    Add any additional fields required only during creation here.
    """
    pass


class {{ model_name }}Update(BaseModel):
    """
    Schema for updating an existing {{ model_name }}.
    Used in PUT/PATCH requests. All fields are optional.

    TODO: Update these fields to match {{ model_name }}Base
    """
    name: Optional[str] = Field(None, description="Name", min_length=1, max_length=100)
    description: Optional[str] = Field(None, description="Description")
    is_active: Optional[bool] = Field(None, description="Active status")


class {{ model_name }}InDB({{ model_name }}Base):
    """
    Schema for {{ model_name }} as stored in database.
    Includes additional fields like id, created_at, updated_at.
    """
    id: int = Field(..., description="Unique identifier")
    created_at: datetime = Field(default_factory=datetime.now, description="Creation timestamp")
    updated_at: datetime = Field(default_factory=datetime.now, description="Last update timestamp")

    class Config:
        from_attributes = True


class {{ model_name }}Response({{ model_name }}InDB):
    """
    Schema for {{ model_name }} in API responses.
    Used in GET requests.
    """
    pass
